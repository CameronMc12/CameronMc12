# Local only. Nothing in the current renderer set needs numpy, Pillow or a font
# file — every section is drawn from data, so `requirements.txt` covers CI and
# local work alike. Kept as a placeholder in case a raster pipeline comes back
# (the archived v1 ASCII portrait and 3D wordmark needed both, plus a macOS
# font; see _archive/v1-float/).
-r requirements.txt
